{
    "title": "Falabella Product Search",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
            "searchFor": {
                "title": "Search For",
                "type": "string",
                "description": "What do you want to search for?",
                "editor": "select",
                "prefill": "items",
                "enum": ["items", "pages"],
                "enumTitles": ["Items (Individual Products)", "Pages (Search Result Pages)"]
            },
        "searchQuery": {
            "title": "Search Query",
            "type": "string",
            "description": "What product are you looking for? (e.g., laptop, smartphone, sofa)",
            "editor": "textfield",
            "example": "laptop"
        },
            "maxProducts": {
                "title": "Maximum Products/Pages",
                "type": "integer",
                "description": "For Items: max products to scrape. For Pages: number of pages to scrape (e.g., 5 = scrape pages 1-5)",
                "minimum": 1,
                "prefill": 25
            },
        "minPrice": {
            "title": "Minimum Price (COP)",
            "type": "integer",
            "description": "Filter products by minimum price in Colombian Pesos (optional)",
            "minimum": 0
        },
        "maxPrice": {
            "title": "Maximum Price (COP)",
            "type": "integer",
            "description": "Filter products by maximum price in Colombian Pesos (optional)",
            "minimum": 0
        }
    },
    "required": ["searchQuery","searchFor","maxProducts"]
}
